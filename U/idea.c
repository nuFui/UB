#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

uint16_t stack[UINT16_MAX];

enum {
  INSTRUCTION_HALT = 0,
  INSTRUCTION_PUSH = 1
};

int main(int argc, char *argv[])
{
  if (argc > 1)
  {
    
  }
}